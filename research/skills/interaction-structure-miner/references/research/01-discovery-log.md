# 01 · Discovery Log（2026-08-30 凌晨，Arena session 01a053b1-123）

## 起点
任务：找一个全网零提及的科研点。约束：任何检索命中即作废或深挖。
锚点：孙承泽的风电场偏航优化项目（FLORIS 仿真：两机 +8.13%，3×3 贪心 +24.04%，POD 两阶 97.97%，PPO MAE 0.523%）。
疑问：为什么贪心这么好？——悬而未决、结果在手、理论缺位。

## 假设与证伪时间线
1. **H1 次模性假设**（P 次模 ⇒ 贪心 (1−1/e) 保证）
   - 检索：排布问题次模性已被 Zhang 2011 证明（不冲突）；偏航次模性 arXiv/OpenAlex/中文 零命中。
   - **被符号分析证伪**：线性叠加高斯模型 ∂²P/∂γ1∂γ2 的交叉项 +6u·u1'·u2' > 0 —— 决策互补而非替代。H1 作废。
2. **H2 互补/替代相结构**（存活，成为主点）
   - 闭式：M_ij = B_ij − A_ij；两机→替代；共享下游→互补；链相邻对→相翻转。
   - FLORIS 验证：两机 M12 恒负✓；三机链 (1,2) 符号随工作点翻转（+0.674 → −0.215）✓；3×3 符号矩阵复现尾流 DAG ✓。
3. **H3 可重入相模式**（g3=30 行 +++−++ 模式）
   - **被数值证伪**：减小步长后发散 + FLORIS 负速度警告 → 模型边界伪影，撤销声明。
4. **H4 最优点解耦**（Law 1）——数值发现，od/diag 0.36→0.023 等多案例、多模型、AEP 版复现；部分机制（stationarity identity）。
5. **H5 贪心几乎最优**——12 随机布局均值 gap 0.019%；交互能界（Theorem 2）同量级复现。
6. **H6 单调比较静态**——TI 扫描 γ1* 严格单调降；格论机制解释 Wynn/King/Quick 的观察。
7. **H7 逆问题**——最优剖面射线响应单调；二分精确反演误差 ~1e-7 kW；对比双线性代理 60.3 kW。
8. **H8 DJS 与符号聚类**——2-3 轮扫描收敛（gap ≤ 0.005%）；5×5 聚类 60s vs 集中式 317s，gap 0.001%。

## 方法论要点
- 先符号后数值：符号分析在写第一行代码前就杀掉了 H1（省了一整晚）。
- 每一步数值声明都要做"步长收敛 + 模型边界"双检查（H3 的教训）。
- 相邻先例必须检索到具体论文并写清区别（Zhang/Starke/WGWD/APC/Bestehorn）。
